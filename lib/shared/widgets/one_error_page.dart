import 'package:flutter/widgets.dart';

class OneErrorPage extends StatelessWidget {
  const OneErrorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
